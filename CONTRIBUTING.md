# Руководство по контрибуции

Спасибо за интерес к проекту Galeries! Мы рады любому вкладу в развитие системы.

## 📋 Содержание

- [Кодекс поведения](#кодекс-поведения)
- [Как внести вклад](#как-внести-вклад)
- [Стандарты кода](#стандарты-кода)
- [Процесс разработки](#процесс-разработки)
- [Отчеты об ошибках](#отчеты-об-ошибках)
- [Предложения новых функций](#предложения-новых-функций)

## 🤝 Кодекс поведения

Участвуя в проекте, вы соглашаетесь соблюдать наш кодекс поведения:

- Будьте уважительны к другим участникам
- Конструктивно критикуйте код, а не людей
- Принимайте конструктивную критику
- Фокусируйтесь на том, что лучше для сообщества

## 🚀 Как внести вклад

### 1. Fork репозитория

Создайте fork репозитория на GitHub и клонируйте его локально:

\`\`\`bash
git clone https://github.com/your-username/galeries.git
cd galeries
git remote add upstream https://github.com/original-owner/galeries.git
\`\`\`

### 2. Создайте ветку для изменений

\`\`\`bash
git checkout -b feature/your-feature-name
# или
git checkout -b fix/your-bug-fix
\`\`\`

### 3. Внесите изменения

- Следуйте [стандартам кода](#стандарты-кода)
- Добавьте тесты для новой функциональности
- Обновите документацию при необходимости

### 4. Commit изменений

Используйте понятные commit сообщения:

\`\`\`bash
git commit -m "feat: добавлена функция X"
git commit -m "fix: исправлена ошибка Y"
git commit -m "docs: обновлена документация Z"
\`\`\`

**Типы commit:**
- `feat:` - новая функциональность
- `fix:` - исправление ошибки
- `docs:` - изменения в документации
- `style:` - форматирование кода
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов
- `chore:` - обновление зависимостей, конфигурации

### 5. Push и создание Pull Request

\`\`\`bash
git push origin feature/your-feature-name
\`\`\`

Создайте Pull Request на GitHub с описанием изменений.

## 💻 Стандарты кода

### TypeScript/JavaScript

- Используйте TypeScript для всех новых файлов
- Следуйте ESLint правилам проекта
- Используйте функциональные компоненты React
- Предпочитайте `const` и `let` вместо `var`
- Используйте async/await вместо промисов где возможно

**Пример:**

\`\`\`typescript
// ✅ Хорошо
export async function fetchGalleries(): Promise<Gallery[]> {
  const supabase = await createClient()
  const { data, error } = await supabase
    .from('galleries')
    .select('*')
  
  if (error) throw error
  return data
}

// ❌ Плохо
export function fetchGalleries() {
  return createClient().then(supabase => {
    return supabase.from('galleries').select('*').then(result => {
      if (result.error) throw result.error
      return result.data
    })
  })
}
\`\`\`

### Python

- Следуйте PEP 8
- Используйте type hints
- Документируйте функции с docstrings
- Используйте async/await для асинхронных операций

**Пример:**

\`\`\`python
# ✅ Хорошо
async def recognize_faces(image_url: str) -> List[FaceDescriptor]:
    """
    Распознает лица на изображении.
    
    Args:
        image_url: URL изображения для обработки
        
    Returns:
        Список дескрипторов найденных лиц
    """
    faces = await detector.detect(image_url)
    return [FaceDescriptor.from_detection(f) for f in faces]

# ❌ Плохо
def recognize_faces(image_url):
    faces = detector.detect(image_url)
    return [FaceDescriptor.from_detection(f) for f in faces]
\`\`\`

### React компоненты

- Используйте функциональные компоненты
- Используйте хуки (useState, useEffect, etc.)
- Разделяйте логику и представление
- Используйте shadcn/ui компоненты где возможно

**Пример:**

\`\`\`typescript
// ✅ Хорошо
export function GalleryCard({ gallery }: { gallery: Gallery }) {
  const [isLoading, setIsLoading] = useState(false)
  
  const handleDelete = async () => {
    setIsLoading(true)
    try {
      await deleteGallery(gallery.id)
    } finally {
      setIsLoading(false)
    }
  }
  
  return (
    <Card>
      <CardHeader>
        <CardTitle>{gallery.title}</CardTitle>
      </CardHeader>
      <CardFooter>
        <Button onClick={handleDelete} disabled={isLoading}>
          Удалить
        </Button>
      </CardFooter>
    </Card>
  )
}
\`\`\`

## 🔄 Процесс разработки

### Локальная разработка

1. Установите зависимости:
\`\`\`bash
npm install
cd python && pip install -r requirements.txt
\`\`\`

2. Настройте переменные окружения (см. DOWNLOAD-INSTRUCTIONS.md)

3. Запустите оба сервера:
\`\`\`bash
# Terminal 1
npm run dev

# Terminal 2
cd python && python main.py
\`\`\`

### Тестирование

Перед созданием PR убедитесь, что:

- [ ] Код компилируется без ошибок
- [ ] ESLint не выдает ошибок
- [ ] TypeScript проверка проходит успешно
- [ ] Все существующие функции работают
- [ ] Новая функциональность протестирована вручную

\`\`\`bash
npm run lint
npm run typecheck
\`\`\`

## 🐛 Отчеты об ошибках

При создании issue об ошибке включите:

1. **Описание проблемы** - что произошло и что ожидалось
2. **Шаги для воспроизведения** - как повторить ошибку
3. **Окружение** - версия браузера, ОС, Node.js, Python
4. **Скриншоты** - если применимо
5. **Логи** - ошибки из консоли браузера или терминала

**Шаблон:**

\`\`\`markdown
## Описание
Краткое описание проблемы

## Шаги для воспроизведения
1. Перейти на страницу X
2. Нажать на кнопку Y
3. Увидеть ошибку Z

## Ожидаемое поведение
Что должно было произойти

## Фактическое поведение
Что произошло на самом деле

## Окружение
- Браузер: Chrome 120
- ОС: Windows 11
- Node.js: 18.17.0
- Python: 3.9.7

## Скриншоты
[Прикрепите скриншоты]

## Логи
\`\`\`
[Вставьте логи из консоли]
\`\`\`
\`\`\`

## 💡 Предложения новых функций

При предложении новой функции опишите:

1. **Проблему** - какую проблему решает функция
2. **Решение** - как функция решает проблему
3. **Альтернативы** - какие есть альтернативные решения
4. **Дополнительный контекст** - скриншоты, примеры из других систем

## 📝 Документация

При добавлении новой функциональности обновите:

- README.md - если изменился процесс установки или основные функции
- ARCHITECTURE.md - если изменилась архитектура
- CHANGELOG.md - добавьте запись о изменении
- Комментарии в коде - документируйте сложную логику

## ❓ Вопросы

Если у вас есть вопросы:

1. Проверьте существующую документацию
2. Поищите в закрытых issues
3. Создайте новый issue с меткой "question"

## 🙏 Благодарности

Спасибо всем, кто вносит вклад в проект!

---

**Версия:** 0.7.8
**Последнее обновление:** 2025-01-31
