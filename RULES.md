# ПРАВИЛА ПРОЕКТА PADEL GALLERIES

Эти правила обязательны для всех AI-ассистентов, работающих с проектом.

---

## 🔴 КРИТИЧЕСКИЕ

Нарушение = потеря времени владельца. Недопустимо.

### 1. ДУМАТЬ → ДЕЛАТЬ → ПРОВЕРЯТЬ
Время владельца дороже всех других ресурсов. Ошибки и переделки недопустимы.

### 2. АУДИТ ≠ ДЕЙСТВИЕ
- Задача "проверь/найди/аудит" → **только анализ и отчёт**
- Код менять **ТОЛЬКО после явного согласования**
- Не делать того, о чём не просили

### 3. ПРАВИЛО РЕДАКТИРОВАНИЯ ФАЙЛОВ
- **< 50% изменений** → точечные правки: скачать → `str_replace` → запушить
- **> 50% изменений** → можно перегенерировать целиком
- **Новые файлы** → создавать напрямую

### 4. КОНКРЕТНАЯ vs ОБЩАЯ ЗАДАЧА
- **Конкретная задача** ("замени X на Y", "исправь ошибку Z") → делать сразу, без вопросов
- **Общая задача / есть выбор** ("найди проблему", несколько вариантов решения) → показать варианты, ждать решения

---

## 🟠 АРХИТЕКТУРНЫЕ

Нарушение = технический долг.

### 5. FRONTEND ↔ BACKEND ↔ DATABASE
Фронтенд общается **ТОЛЬКО через бэкенд API**, никогда напрямую с базой данных.

```
Frontend (Vercel) → Backend API (FastAPI) → Supabase
```

### 6. ЕДИНЫЙ ФОРМАТ ОТВЕТОВ
Все эндпоинты бэкенда возвращают `ApiResponse`:
```json
{
  "success": true,
  "data": { ... },
  "error": null
}
```

### 7. ФРОНТЕНД ЧИТАЕТ ИЗ `result.data`
```typescript
// ПРАВИЛЬНО:
const clusters = result.data?.clusters || []

// НЕПРАВИЛЬНО:
const clusters = result.clusters || []
```

### 8. SINGLE SOURCE OF TRUTH
Одна точка истины для каждой сущности. Не дублировать данные.

---

## 🟡 ПРОЦЕССНЫЕ

Нарушение = хаос в проекте.

### 9. ПОЛНЫЙ ПОИСК ПРИ ЗАМЕНЕ
Если меняется поле/название/API:
1. Искать **ВСЕ вхождения** во **ВСЕХ файлах**
2. Не останавливаться на первом найденном
3. Составить полный список → показать → получить согласие → менять всё

### 10. ПОСЛЕ ИЗМЕНЕНИЙ — ПРОВЕРКА
```bash
cd ~/python && git pull && ./restart.sh
```
Затем тестировать изменённый функционал.

---

## ИНФРАСТРУКТУРА

| Компонент | URL / Расположение |
|-----------|-------------------|
| Frontend | Vercel (автодеплой из main) |
| Backend | http://vlcpadel.com:8001 |
| Database | Supabase PostgreSQL |
| Backend код | ~/python на Hetzner |

---

*Последнее обновление: 2025-12-23*
