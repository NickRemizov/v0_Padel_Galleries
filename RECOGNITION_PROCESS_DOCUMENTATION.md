# Полное описание процесса распознавания лиц в системе Padel Galleries

**Версия:** 4.0.0  
**Дата:** 16 декабря 2025  
**Статус:** Актуальная документация системы

> **Краткое резюме для быстрого ознакомления:** см. `docs/RECOGNITION_SUMMARY.md`

---

## Содержание

1. [Архитектура системы](#архитектура-системы)
2. [Процесс распознавания (пошагово)](#процесс-распознавания-пошагово)
3. [Статусы фото (бейджи)](#статусы-фото-бейджи)
4. [Работа с неизвестными лицами](#работа-с-неизвестными-лицами)
5. [Настройки качества](#настройки-качества)
6. [Текущая реализация](#текущая-реализация)
7. [Что нужно доделать](#что-нужно-доделатьизменить)
8. [Инструкции для администратора](#инструкции-для-администратора)
9. [Ключевые понятия](#ключевые-понятия)

---

## Архитектура системы

### Общая схема

\`\`\`
┌─────────────────────────────────────────────────────────────┐
│                    Next.js Frontend (Vercel)                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │ Админ-панель │  │ Server       │  │ API Routes   │       │
│  │ компоненты   │  │ Actions      │  │ (proxy)      │       │
│  └──────────────┘  └──────────────┘  └──────────────┘       │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│              FastAPI Backend (Hetzner vlcpadel.com:8001)     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │ /detect-faces│  │ /recognize   │  │ /training/*  │       │
│  │              │  │              │  │              │       │
│  └──────────────┘  └──────────────┘  └──────────────┘       │
│                              │                               │
│              ┌───────────────┼───────────────┐              │
│              ▼               ▼               ▼              │
│      ┌──────────────┐ ┌──────────────┐ ┌──────────────┐    │
│      │  InsightFace │ │   HNSWLIB    │ │   HDBSCAN    │    │
│      │  (детекция)  │ │   (поиск)    │ │ (кластеры)   │    │
│      └──────────────┘ └──────────────┘ └──────────────┘    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Supabase PostgreSQL                       │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │ gallery_     │  │ photo_faces  │  │   people     │       │
│  │ images       │  │ +descriptor  │  │              │       │
│  └──────────────┘  └──────────────┘  └──────────────┘       │
└─────────────────────────────────────────────────────────────┘
\`\`\`

### Frontend (Next.js 15 + React 19)

**Основные компоненты:**

| Компонент | Описание |
|-----------|----------|
| `gallery-images-manager.tsx` | Управление галереей фото, бейджи статусов |
| `auto-recognition-dialog.tsx` | Диалог пакетного распознавания |
| `unknown-faces-review-dialog.tsx` | Работа с кластерами неизвестных лиц |
| `face-tagging-dialog.tsx` | Ручная верификация одного фото |
| `face-training-manager.tsx` | Настройки качества распознавания |
| `database-integrity-checker.tsx` | Проверка целостности БД |

### Backend (FastAPI + Python)

| Модуль | Описание |
|--------|----------|
| `main.py` | Entry point, роутеры |
| `routers/recognition/` | Детекция и распознавание лиц |
| `routers/training.py` | Обучение и кластеризация |
| `routers/faces.py` | CRUD для photo_faces |
| `services/face_recognition.py` | InsightFace логика |
| `services/hnsw_index.py` | HNSWLIB индекс |

### База данных

> **Полная схема:** `docs/DATABASE_SCHEMA.md`

**Ключевые таблицы для распознавания:**

| Таблица | Описание |
|---------|----------|
| `gallery_images` | Фотографии в галереях |
| `photo_faces` | Лица на фото + эмбеддинги (512-мерные) |
| `people` | Игроки |
| `face_recognition_config` | Настройки распознавания |

**Важные поля в `photo_faces`:**

| Поле | Тип | Описание |
|------|-----|----------|
| `photo_id` | uuid | FK → gallery_images |
| `person_id` | uuid | FK → people (nullable) |
| `insightface_descriptor` | vector(512) | **Эмбеддинг лица** |
| `insightface_bbox` | jsonb | Координаты лица [x1, y1, x2, y2] |
| `insightface_confidence` | float | Уверенность детекции (0-1) |
| `recognition_confidence` | float | Уверенность распознавания (0-1) |
| `verified` | boolean | Подтверждено вручную |
| `blur_score` | float | Резкость (Laplacian variance) |

> ⚠️ **DEPRECATED:** Таблица `face_descriptors` переименована в `face_descriptors_DEPRECATED`.
> Все эмбеддинги теперь хранятся в `photo_faces.insightface_descriptor`.

---

## Процесс распознавания (пошагово)

### Этап 1: Загрузка фото

\`\`\`
Админ → Drag&Drop / кнопка → Vercel Blob Storage → URL в gallery_images
\`\`\`

**Что происходит:**
1. Фото загружается в Vercel Blob Storage
2. Создаётся запись в `gallery_images` с `has_been_processed = false`
3. Фото появляется в галерее **без бейджа**

### Этап 2: Детекция лиц

\`\`\`
Кнопка "Распознать фото" 🔍 
    → /api/face-detection/detect (Next.js proxy)
    → FastAPI /detect-faces
    → InsightFace детекция
\`\`\`

**Что происходит:**
1. Загрузка фото по URL
2. InsightFace находит все лица на фото
3. Для каждого лица вычисляется:
   - `bbox` — координаты прямоугольника
   - `blur_score` — резкость (Laplacian variance)
   - `insightface_confidence` — уверенность что это лицо
   - `embedding` — 512-мерный вектор (дескриптор)

**Фильтрация (если включен чекбокс "Применять настройки качества"):**
- Отбрасываются лица меньше `min_face_size`
- Отбрасываются лица с `blur_score` < `min_blur_score`
- Отбрасываются лица с `insightface_confidence` < `min_detection_score`

### Этап 3: Распознавание лиц

\`\`\`
Для каждого обнаруженного лица:
    → FastAPI /recognize-face
    → HNSWLIB поиск похожих среди верифицированных
    → Возврат person_id + confidence
\`\`\`

**Что происходит:**
1. Эмбеддинг сравнивается с индексом верифицированных лиц
2. Если найдено похожее лицо с `similarity > threshold`:
   - Присваивается `person_id`
   - Устанавливается `recognition_confidence`
3. Если не найдено — `person_id = null` (неизвестное лицо)

**Пороги распознавания:**
- Для верифицированных лиц: 0.6
- Для неверифицированных: 0.75 (строже)

### Этап 4: Сохранение результатов

**Что сохраняется в `photo_faces`:**
- `photo_id` — ID фото
- `person_id` — ID игрока (или null)
- `insightface_descriptor` — 512-мерный эмбеддинг
- `insightface_bbox` — координаты
- `insightface_confidence` — уверенность детекции
- `recognition_confidence` — уверенность распознавания
- `blur_score` — резкость
- `verified = false` — автоматическое распознавание

**Обновляется `gallery_images`:**
- `has_been_processed = true`

---

## Статусы фото (бейджи)

| Бейдж | Цвет | Условие | Описание |
|-------|------|---------|----------|
| *Без бейджа* | — | `has_been_processed = false` | Новое фото, не обработано |
| **NFD** | Серый | Нет записей в `photo_faces` | No Faces Detected |
| **2/4** | Оранжевый | 2 лица без `person_id` из 4 | Есть неопознанные |
| **75%** | Синий | Минимальный `recognition_confidence` | Распознано, не подтверждено |
| **✓** | Зелёный | Все `verified = true` или `confidence = 1.0` | Полностью верифицировано |

**Логика определения бейджа:**
\`\`\`
if (!has_been_processed) → без бейджа
else if (no faces) → NFD
else if (any face without person_id) → XX/YY
else if (any face with verified=false) → min confidence %
else → ✓
\`\`\`

---

## Работа с неизвестными лицами

### Кластеризация

\`\`\`
Кнопка "Неизвестные лица" 👤+
    → FastAPI /training/cluster-unverified-faces
    → HDBSCAN группировка по similarity
    → Возврат кластеров в UI
\`\`\`

**Что происходит:**
1. Загружаются все лица с `person_id = null` из галереи
2. HDBSCAN группирует похожие лица в кластеры
3. Кластеры сортируются по размеру (от большего к меньшему)
4. Единичные лица (label=-1) добавляются как кластеры размером 1

### Действия с кластерами

| Кнопка | Действие |
|--------|----------|
| **"Создать игрока"** | Создать нового игрока, присвоить ему все фото кластера |
| **"Выбрать игрока"** | Присвоить кластер существующему игроку |
| **"Зрители"** | Пометить весь кластер как зрителей (не игроки) |
| **"X" на фото** | Исключить конкретное фото из кластера |

### Логика при присвоении кластера

При создании/выборе игрока для всех фото кластера:
- `person_id` = ID игрока
- `recognition_confidence` = 1.0 (100%)
- `verified` определяется по логике:
  - Если на фото только это лицо → `verified = true`
  - Если на фото несколько лиц и все остальные верифицированы → `verified = true`
  - Иначе → `verified = false`

### Зрители

**Когда использовать:** Реальные зрители, люди которые не должны быть в базе игроков.

**Что происходит:**
- `person_id` остаётся `null`
- Лица не участвуют в дальнейшей кластеризации
- Эмбеддинги могут быть удалены (опционально)

---

## Настройки качества

> **Где:** Админпанель → Face Training Manager → Quality Filters

### Фильтры качества

| Настройка | Диапазон | По умолчанию | Описание |
|-----------|----------|--------------|----------|
| **Min Face Size** | 20-100 px | 80 px | Минимальный размер лица |
| **Min Blur Score** | 30-150 | 100 | Минимальная резкость |
| **Min Detection Score** | 0.5-0.95 | 0.7 | Минимальная уверенность детекции |

### Рекомендации

**Min Face Size:**
- 20-40 px: Очень маленькие лица (низкое качество)
- 60-80 px: Нормальные лица (рекомендуется)
- 100+ px: Только крупные лица

**Min Blur Score:**
- 30-60: Размытые лица допускаются
- 80-100: Нормальная резкость (рекомендуется)
- 120-150: Только чёткие лица

**Важно:** Blur score зависит от размера лица. Маленькие лица имеют низкий blur score даже при хорошей резкости.

**Min Detection Score:**
- 0.5-0.6: Много ложных срабатываний
- 0.7-0.8: Рекомендуется
- 0.85-0.95: Только очевидные лица

---

## Текущая реализация

### ✅ Что работает

1. **Загрузка фото**
   - Drag & Drop (оптимизирован, без мерцания)
   - Кнопка загрузки
   - Сохранение в Vercel Blob

2. **Пакетное распознавание**
   - Детекция через InsightFace
   - Распознавание через HNSWLIB
   - Применение фильтров качества
   - Чекбокс "Применять настройки качества"

3. **Статусы фото (бейджи)**
   - Все бейджи работают корректно
   - `has_been_processed` обновляется автоматически

4. **Кластеризация неизвестных лиц**
   - HDBSCAN кластеризация
   - Отображение кластеров
   - Исключение фото из кластера
   - Создание нового игрока
   - Выбор существующего игрока

5. **Ручная верификация**
   - Диалог FaceTaggingDialog
   - Назначение игроков лицам
   - Сохранение с `verified = true`

6. **Настройки качества**
   - UI в админпанели
   - Сохранение в БД
   - Применение при распознавании

7. **Перестроение индекса HNSWLIB**
   - Автоматическое после изменений
   - Только верифицированные лица в индексе

---

## Что нужно доделать/изменить

### Высокий приоритет

#### 1. Учёт контекста в распознавании

**Текущее состояние:** Настройка `context_weight` есть в БД, но логика не реализована.

**Что нужно:**
- Передавать `photo_id` и `gallery_id` в `recognize_face()`
- Проверять: есть ли этот игрок на других фото галереи?
- Повышать confidence на `context_weight` при совпадении

**Пример:** Если Иван уже на 3 фото этой галереи, и AI нашёл похожее лицо с confidence 0.65, то итоговый confidence = 0.65 + 0.1 (context_weight) = 0.75

#### 2. Top-matches в деталях распознавания

**Текущее состояние:** UI готов, но данные не приходят с бэкенда.

**Что нужно:**
- Backend должен возвращать топ-3 похожих лиц с именами и similarity
- Показывать в детальном окне распознавания

#### 3. Настройка порога для персональных галерей

**Что нужно:**
- Добавить `min_confidence_for_gallery` в настройки (по умолчанию 0.80)
- В публичных галереях показывать только фото с `verified = true` ИЛИ `confidence >= порог`
- В админпанели показывать все фото

### Средний приоритет

#### 4. Адаптивный blur_score

**Идея:** Учитывать размер лица при оценке blur_score. Маленькие лица всегда имеют низкий blur_score.

#### 5. Автообучение по расписанию

**Идея:** Переобучать HNSWLIB индекс ночью автоматически.

---

## Инструкции для администратора

### Базовый процесс работы

#### 1. Загрузка фото
1. Открой галерею через кнопку "Images" 📷
2. Загрузи фото (drag & drop или кнопка)
3. Фото появятся без бейджа

#### 2. Пакетное распознавание
1. Нажми "Распознать фото" 🔍
2. Оставь чекбокс "Применять настройки качества" включённым
3. Дождись завершения
4. Проверь бейджи:
   - ✓ (зелёный) — готово
   - XX% (синий) — нужна верификация
   - XX/YY (оранжевый) — есть неизвестные

#### 3. Работа с неизвестными
1. Нажми "Неизвестные лица" 👤+
2. Для каждого кластера:
   - Проверь, один ли человек на всех фото
   - Исключи ошибочные фото кнопкой "X"
   - Выбери действие:
     - **Создать игрока** — если новый
     - **Выбрать игрока** — если уже есть
     - **Зрители** — если не игрок

#### 4. Ручная верификация
1. Кликни на фото с бейджем XX% или XX/YY
2. Назначь игрока каждому лицу
3. Нажми "Сохранить"
4. Фото получит статус ✓

### Частые вопросы

**Q: Почему лицо не распознаётся?**
A: Возможные причины:
- Лицо слишком маленькое (< Min Face Size)
- Лицо размытое (< Min Blur Score)
- Игрока нет в базе — создай через "Неизвестные лица"

**Q: Как распознать без фильтров качества?**
A: Отключи чекбокс "Применять настройки качества" перед распознаванием.

**Q: Что если AI ошибся в кластере?**
A: Нажми "X" на ошибочных фото — они не будут присвоены игроку.

**Q: Почему в галерее игрока не все фото?**
A: В публичной галерее показываются только:
- Верифицированные (✓)
- Или с confidence >= 80%

Решение: Вручную верифицируй нужные фото.

---

## Ключевые понятия

| Термин | Описание |
|--------|----------|
| **Эмбеддинг** | 512-мерный вектор, описывающий лицо |
| **BBox** | Bounding Box — координаты прямоугольника вокруг лица |
| **Confidence** | Уверенность (0-1) в правильности распознавания |
| **Verified** | Подтверждено вручную администратором |
| **HNSWLIB** | Индекс для быстрого поиска похожих эмбеддингов |
| **HDBSCAN** | Алгоритм кластеризации для группировки похожих лиц |
| **InsightFace** | Библиотека для детекции и распознавания лиц |
| **Blur Score** | Резкость лица (Laplacian variance) |

---

## История изменений документа

| Версия | Дата | Изменения |
|--------|------|-----------|
| 4.0.0 | 16.12.2025 | Полная актуализация: исправлены названия таблиц, обновлена архитектура, синхронизировано с DATABASE_SCHEMA.md |
| 3.4.0 | 31.10.2025 | Добавлены настройки качества, статусы фото |
| 3.0.0 | Октябрь 2025 | Начальная версия документа |

---

**Связанные документы:**
- `docs/RECOGNITION_SUMMARY.md` — краткое резюме (5 минут)
- `docs/DATABASE_SCHEMA.md` — схема БД
- `docs/PROJECT_CONTEXT.md` — общий контекст проекта
- `docs/INTEGRITY_CHECKER.md` — проверка целостности данных
