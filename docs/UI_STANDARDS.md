# UI Standards / Стандарты UI

Документ описывает единые стандарты пользовательского интерфейса для админ-панели VLC Padel Galleries.

## Иконки действий

### 🗑️ Корзина (Trash2)
**Значение:** Физическое удаление из базы данных

**Используется для:**
- Удаление галереи (вместе со всеми фото)
- Удаление игрока из базы
- Удаление фотографии из галереи
- Удаление площадки, организатора, фотографа

**Пример:**
\`\`\`tsx
import { Trash2 } from "lucide-react"

<Button variant="destructive" size="icon">
  <Trash2 className="h-4 w-4" />
</Button>
\`\`\`

---

### ✕ Крестик (X)
**Значение:** Разрыв связи / удаление из списка (без физического удаления)

**Используется для:**
- Убрать игрока с фото (разрыв person_id, дескриптор остаётся)
- Убрать лицо из кластера
- Убрать лицо из детекций на фото
- Закрытие диалогов

**Пример:**
\`\`\`tsx
import { X } from "lucide-react"

<Button variant="destructive" size="icon">
  <X className="h-4 w-4" />
</Button>
\`\`\`

---

## Информационные бейджи

### ✓ Зелёная галочка в кружке (информация)
**Значение:** Статус "верифицировано" / "всё ОК"

**Характеристики:**
- Форма: круглая (`rounded-full`)
- Цвет: зелёный (`bg-green-500`)
- Размер: 6-8 (24-32px)
- НЕ кликабельно

**Пример:**
\`\`\`tsx
<div className="bg-green-500 text-white rounded-full w-8 h-8 flex items-center justify-center">
  ✓
</div>
\`\`\`

---

### ✓ Зелёная галочка в квадрате (кнопка)
**Значение:** Действие подтверждения / верификации

**Характеристики:**
- Форма: квадратная (стандартный Button)
- Цвет: зелёный (`bg-green-500`)
- Кликабельно

**Пример:**
\`\`\`tsx
import { Check } from "lucide-react"

<Button
  variant="secondary"
  size="icon"
  className="bg-green-500 hover:bg-green-600 text-white"
>
  <Check className="h-4 w-4" />
</Button>
\`\`\`

---

## Диалоги подтверждения

### ❌ Запрещено: window.confirm()
Системные диалоги `confirm()` НЕ используются.

### ✅ Обязательно: React AlertDialog
Все диалоги подтверждения используют компонент `DeleteConfirmDialog` или `AlertDialog`.

**Расположение компонента:**
\`\`\`
components/ui/delete-confirm-dialog.tsx
\`\`\`

**Пример использования:**
\`\`\`tsx
import { DeleteConfirmDialog } from "@/components/ui/delete-confirm-dialog"

const [deleteConfirm, setDeleteConfirm] = useState<{ open: boolean; item: Item | null }>({
  open: false,
  item: null,
})

// Открытие диалога
function handleDeleteClick(item: Item) {
  setDeleteConfirm({ open: true, item })
}

// Подтверждение
async function handleDeleteConfirm() {
  if (!deleteConfirm.item) return
  // ... логика удаления
  setDeleteConfirm({ open: false, item: null })
}

// JSX
<DeleteConfirmDialog
  open={deleteConfirm.open}
  onOpenChange={(open) => setDeleteConfirm({ open, item: open ? deleteConfirm.item : null })}
  onConfirm={handleDeleteConfirm}
  description={`Удалить "${deleteConfirm.item?.name}"?`}
/>
\`\`\`

---

## Сводная таблица

| Элемент | Форма | Цвет | Действие |
|---------|-------|------|----------|
| Trash2 | квадрат | красный | Физическое удаление |
| X | квадрат | красный | Разрыв связи |
| ✓ badge | круг | зелёный | Информация (статус) |
| Check button | квадрат | зелёный | Подтверждение |
| AlertDialog | - | - | Все подтверждения |

---

## Чеклист для ревью

При добавлении нового функционала удаления проверить:

- [ ] Физическое удаление → иконка `Trash2`
- [ ] Разрыв связи → иконка `X`
- [ ] Подтверждение → `DeleteConfirmDialog` (не `confirm()`)
- [ ] Статус верификации → круглый бейдж с ✓
- [ ] Кнопка верификации → квадратная кнопка с `Check`

---

*Последнее обновление: 2026-01-01*
