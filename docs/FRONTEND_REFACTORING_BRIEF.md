# Frontend Refactoring Brief

> **Цель:** Разбить большие компоненты на меньшие файлы
> **Дата:** 28 декабря 2025
> **Для:** Следующий чат с AI

---

## 🎯 Проблема

Многие компоненты в `components/admin/` слишком большие (>20 KB), что:
- Затрудняет понимание кода
- Усложняет точечные правки
- Увеличивает риск ошибок при изменениях
- Делает код менее переиспользуемым

---

## 📊 Топ-10 файлов для рефакторинга

| # | Файл | Размер | Приоритет |
|---|------|--------|----------|
| 1 | `gallery-images-manager.tsx` | 39 KB | 🔴 Высокий |
| 2 | `face-tagging-dialog.tsx` | 33 KB | 🔴 Высокий |
| 3 | `database-integrity-checker.tsx` | 32 KB | 🟡 Средний |
| 4 | `person-gallery-dialog.tsx` | 31 KB | 🔴 Высокий |
| 5 | `face-training-manager.tsx` | 30 KB | 🟡 Средний |
| 6 | `training-stats-card.tsx` | 29 KB | 🟡 Средний |
| 7 | `batch-recognition-dialog.tsx` | 19 KB | 🟢 Низкий |
| 8 | `consistency-audit-dialog.tsx` | 18 KB | 🟢 Низкий |
| 9 | `duplicate-people-dialog.tsx` | 17 KB | 🟢 Низкий |
| 10 | `auto-recognition-dialog.tsx` | 16 KB | 🟢 Низкий |

---

## 🏗️ Паттерн рефакторинга

### Структура папки компонента
```
components/admin/
├── face-tagging/                    # Папка для крупного компонента
│   ├── index.tsx                    # Re-export главного компонента
│   ├── FaceTaggingDialog.tsx        # Главный компонент (оркестратор)
│   ├── components/                  # Подкомпоненты
│   │   ├── FaceBox.tsx             # Отрисовка бокса лица
│   │   ├── FaceList.tsx            # Список лиц
│   │   ├── PersonSelector.tsx      # Выбор игрока
│   │   └── RecognitionControls.tsx # Кнопки управления
│   ├── hooks/                       # Кастомные хуки
│   │   ├── useFaces.ts             # Логика работы с лицами
│   │   └── useRecognition.ts       # Логика распознавания
│   ├── utils/                       # Утилиты
│   │   ├── bbox-calculations.ts    # Расчёты координат
│   │   └── face-styles.ts          # Стили для лиц
│   └── types.ts                     # TypeScript типы
├── face-tagging-dialog.tsx          # DEPRECATED: редирект на новый
└── ...
```

### Правила декомпозиции

1. **Один файл = одна ответственность**
   - Компонент отображения
   - Хук с логикой
   - Утилита с вычислениями

2. **Главный компонент = оркестратор**
   - Собирает подкомпоненты
   - Минимум логики
   - Передаёт props вниз

3. **Хуки для состояния и side effects**
   - useState/useEffect → в кастомный хук
   - API вызовы → в хук
   - Сложные вычисления → в хук с useMemo

4. **Утилиты для чистых функций**
   - Расчёты без side effects
   - Форматирование данных
   - Валидация

---

## 📋 План рефакторинга по файлам

### 1. gallery-images-manager.tsx (39 KB) — ВЫСОКИЙ ПРИОРИТЕТ

**Текущее содержимое:**
- Грид фотографий
- Drag & drop загрузка
- Контекстное меню
- Лайтбокс
- Бейджи распознавания
- Сортировка/фильтрация
- Массовые операции

**Предлагаемая структура:**
```
gallery-images/
├── index.tsx
├── GalleryImagesManager.tsx    # Главный (state, API calls)
├── components/
│   ├── ImageGrid.tsx           # Грид с виртуализацией
│   ├── ImageCard.tsx           # Карточка одного фото
│   ├── ImageBadge.tsx          # Бейдж распознавания
│   ├── UploadZone.tsx          # Drag & drop зона
│   ├── BulkActions.tsx         # Массовые операции
│   └── SortFilterControls.tsx  # Сортировка/фильтры
├── hooks/
│   ├── useGalleryImages.ts     # Загрузка/управление
│   ├── useImageUpload.ts       # Логика загрузки
│   └── useBulkSelection.ts     # Мультиселект
└── types.ts
```

### 2. face-tagging-dialog.tsx (33 KB) — ВЫСОКИЙ ПРИОРИТЕТ

**Текущее содержимое:**
- Канвас с изображением
- Боксы лиц
- Панель лиц справа
- Выбор игрока
- Кнопки распознавания
- Навигация prev/next

**Предлагаемая структура:**
```
face-tagging/
├── index.tsx
├── FaceTaggingDialog.tsx       # Dialog wrapper
├── components/
│   ├── ImageCanvas.tsx         # Канвас + zoom/pan
│   ├── FaceBoxOverlay.tsx      # Отрисовка боксов
│   ├── FacePanel.tsx           # Панель справа
│   ├── FaceCard.tsx            # Карточка одного лица
│   ├── PersonSelector.tsx      # Combobox выбора игрока
│   └── NavigationControls.tsx  # Prev/Next кнопки
├── hooks/
│   ├── useFaceTagging.ts       # Основная логика
│   ├── useImageZoom.ts         # Zoom/pan
│   └── usePersonSearch.ts      # Поиск игроков
├── utils/
│   └── bbox-to-style.ts        # Расчёт стилей боксов
└── types.ts
```

### 3. person-gallery-dialog.tsx (31 KB) — ВЫСОКИЙ ПРИОРИТЕТ

**Текущее содержимое:**
- Грид фото игрока
- Face-centered thumbnails
- Batch verify
- Удаление с фото
- Сортировка
- Навигация к FaceTagging

**Предлагаемая структура:**
```
person-gallery/
├── index.tsx
├── PersonGalleryDialog.tsx
├── components/
│   ├── PhotoGrid.tsx           # Грид фото
│   ├── PhotoCard.tsx           # Карточка фото
│   ├── FaceThumbnail.tsx       # Face-centered thumbnail
│   ├── ActionButtons.tsx       # Verify/Delete/Avatar
│   └── BatchControls.tsx       # Массовые операции
├── hooks/
│   ├── usePersonPhotos.ts      # Загрузка фото
│   └── useBatchVerify.ts       # Batch операции
├── utils/
│   └── face-styles.ts          # calculateFaceStyles()
└── types.ts
```

---

## ⚠️ Правила при рефакторинге

### DO (Делать)
- ✅ Сохранять обратную совместимость импортов
- ✅ Использовать re-exports через index.tsx
- ✅ Выносить типы в отдельный types.ts
- ✅ Документировать props в JSDoc
- ✅ Тестировать после каждого файла

### DON'T (Не делать)
- ❌ Менять API компонентов (props)
- ❌ Менять логику работы
- ❌ Удалять старые файлы до проверки
- ❌ Рефакторить несколько компонентов сразу

---

## 📝 Чеклист для каждого компонента

```markdown
## Рефакторинг: [название]

### Подготовка
- [ ] Изучить текущий код
- [ ] Определить boundaries (что выносить)
- [ ] Согласовать структуру с владельцем

### Выполнение
- [ ] Создать папку компонента
- [ ] Вынести types.ts
- [ ] Вынести утилиты
- [ ] Вынести хуки
- [ ] Вынести подкомпоненты
- [ ] Создать главный компонент-оркестратор
- [ ] Добавить index.tsx с re-export
- [ ] Обновить старый файл (deprecated redirect)

### Проверка
- [ ] Все импорты работают
- [ ] UI не изменился
- [ ] Функционал сохранён
- [ ] TypeScript без ошибок
- [ ] Commit + push
```

---

## 🚀 Порядок работы в чате

1. **Выбрать один компонент** из топ-3
2. **Изучить код** — AI читает текущий файл
3. **Предложить структуру** — AI показывает план декомпозиции
4. **Согласовать** — владелец одобряет или корректирует
5. **Выполнить** — по одному файлу за раз
6. **Проверить** — владелец тестирует в браузере
7. **Следующий компонент** или завершение

---

## 📚 Связанные документы

- `docs/PROJECT_CONTEXT.md` — контекст проекта
- `docs/DATABASE_SCHEMA.md` — схема БД
- `docs/RECOGNITION_SUMMARY.md` — система распознавания

---

*Создано: 28 декабря 2025*
