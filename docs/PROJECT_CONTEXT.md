# PADEL GALLERIES — Контекст проекта для AI

> **Для AI-ассистента:** Прочитай этот документ полностью перед любой работой.
> **Обновлено:** 25 декабря 2025
> **Возраст проекта:** ~2.5 месяца (с октября 2025)

---

## 🎯 О проекте

### Что это
Портал для сообщества падел-игроков, начинающийся с Валенсии (Испания) с планами расширения на другие города и страны.

### Целевая аудитория
Экспаты (иностранцы в Испании). Для испанцев есть Playtomic (в основном про спорт), а экспатам нужна ещё и социализация.

### Текущий функционал
1. **Фотогалереи турниров** — загрузка, просмотр, управление
2. **Распознавание лиц игроков** — InsightFace + HNSWLIB индекс + HDBSCAN кластеризация
3. **Админ-панель** — управление галереями, людьми, тегирование лиц
4. **Мультигородность** — архитектура готова (cities → locations → galleries)

### Планируемый функционал
1. **Авторизация пользователей** — Telegram/Google/Meta OAuth
2. **Социальные функции** — лайки, комментарии, избранное
3. **Бот организации игр** — интеграция существующего Telegram-бота
4. **Система отчётов по турнирам**

---

## 🏗️ Архитектура

### Единственный репозиторий
```
NickRemizov/v0_Padel_Galleries  ← ВСЁ ЗДЕСЬ
├── app/                        # Next.js frontend (App Router)
├── components/                 # React компоненты
├── lib/                        # Утилиты, API клиент
│   └── utils/                 # Утилиты (person-display.ts и др.)
├── python/                     # FastAPI backend (деплоится на Hetzner)
│   ├── routers/               # API endpoints (модульная структура)
│   │   ├── people/           # People API (crud, photos, consistency...)
│   │   ├── galleries.py      # Galleries API
│   │   └── faces.py          # Faces API
│   ├── services/              # Бизнес-логика
│   ├── repositories/          # Работа с БД
│   └── core/                  # Конфигурация, исключения
├── docs/                       # Документация
└── migrations/                 # SQL миграции
```

### ⚠️ ВАЖНО: Padel_Galleries — МЁРТВЫЙ ФОРК
Репозиторий `NickRemizov/Padel_Galleries` — неудачная попытка переезда на собственный PostgreSQL. **Игнорировать полностью.**

### Текущий стек
| Компонент | Технология | Где хостится |
|-----------|------------|--------------|
| Frontend | Next.js 15, React 19 | Vercel |
| Backend | FastAPI + Python 3.11 | Hetzner (vlcpadel.com:8001) |
| База данных | PostgreSQL | Supabase (hosted) |
| Хранилище фото | Vercel Blob | Vercel |
| Face Recognition | InsightFace + HNSWLIB | Hetzner |

### Планируемые изменения
| Что | Сейчас | Станет |
|-----|--------|--------|
| Хранилище фото | Vercel Blob | MinIO на Hetzner |
| Frontend (prod) | Vercel | Hetzner |
| Frontend (dev) | Vercel | Остаётся на Vercel |

---

## ⛔ КРИТИЧЕСКИЕ ПРАВИЛА РАЗРАБОТКИ

### Правило 1: Архитектура данных (ГЛАВНОЕ!)
```
Frontend → Server Actions/API Routes → FastAPI Backend → Supabase
```

**ЗАПРЕЩЕНО:** Прямые вызовы Supabase из frontend-компонентов.
**РАЗРЕШЕНО:** Только через Server Actions (`app/admin/actions/`) или API routes (`app/api/`).

> Сейчас это правило нарушается в ~15 файлах. Миграция в процессе.
> См. `docs/FRONTEND_API_AUDIT.md` для деталей.

### Правило 2: План перед кодом
Перед ЛЮБЫМ изменением:
1. Показать какие файлы будут изменены
2. Показать полный flow данных
3. Дождаться одобрения

**Формат:**
```
ПЛАН:
1. Изменяю файл X, строки Y-Z
2. Flow: компонент → action → endpoint → таблица
3. Затронутые типы: Type1, Type2

Одобряешь?
```

### Правило 3: Проверка перед изменением БД
- [ ] Найти схему таблицы в `docs/DATABASE_SCHEMA.md`
- [ ] Найти существующий код, работающий с этой таблицей
- [ ] Добавить логирование

### Правило 4: Не глушить ошибки
```python
# ❌ ЗАПРЕЩЕНО
try:
    result = await do_something()
except:
    pass

# ✅ ПРАВИЛЬНО
try:
    result = await do_something()
except Exception as e:
    logger.error(f"Error: {e}")
    raise
```

### Правило 5: Не предполагать — проверять
Перед работой с таблицей — проверить её схему через GitHub или SQL.

### Правило 6: Точечные правки
Если меняется <50% файла — использовать str_replace, НЕ перезаписывать целиком.

---

## 📁 Ключевые файлы

### Frontend
| Файл | Описание |
|------|----------|
| `app/admin/actions/*.ts` | Server Actions для админки |
| `lib/apiClient.ts` | Клиент для FastAPI (apiFetch) |
| `lib/utils/person-display.ts` | Утилиты отображения игроков |
| `components/admin/` | Админ-компоненты |

### Backend
| Файл | Описание |
|------|----------|
| `python/main.py` | Entry point FastAPI |
| `python/routers/` | API endpoints |
| `python/routers/people/` | People API (модульная структура) |
| `python/services/` | Бизнес-логика |
| `python/repositories/` | Работа с БД |

### Документация
| Файл | Описание |
|------|----------|
| `docs/PROJECT_CONTEXT.md` | **ЭТО** — главный контекст |
| `docs/DATABASE_SCHEMA.md` | Схема БД (актуальная!) |
| `docs/ROADMAP.md` | План работ и задачи |
| `docs/FRONTEND_API_AUDIT.md` | Прогресс миграции на FastAPI |
| `RECOGNITION_PROCESS_DOCUMENTATION.md` | Детали системы распознавания |

---

## 🗄️ База данных (основные таблицы)

| Таблица | Описание |
|---------|----------|
| `galleries` | Галереи событий |
| `gallery_images` | Фотографии в галереях |
| `people` | Игроки |
| `photo_faces` | Лица на фото + эмбеддинги (512-мерные) |
| `cities` | Города (мультигородность) |
| `locations` | Площадки/клубы |

**Полная схема:** `docs/DATABASE_SCHEMA.md`

---

## 🔧 Серверы и доступы

### Hetzner (Backend)
- **URL:** http://vlcpadel.com:8001
- **Health check:** `curl http://vlcpadel.com:8001/health`
- **Логи:** `tail -f /tmp/fastapi.log`
- **Рестарт:** `~/scripts/run.sh`
- **Деплой:** Автоматический через GitHub Actions

### Vercel (Frontend)
- Auto-deploy при push в main
- Preview deploys для веток

---

## 📋 Текущие задачи

> Полный список с приоритетами: `docs/ROADMAP.md`

### Высокий приоритет
1. **Миграция на FastAPI** — убрать прямые вызовы Supabase с фронтенда
2. **Рефакторинг крупных компонентов** — разбить файлы >20KB

### Средний приоритет
3. **Slug-адреса** — SEO-friendly URLs вместо UUID
4. **Миграция на MinIO** — перенести фото с Vercel Blob

### Низкий приоритет
5. **Авторизация пользователей** — после стабилизации архитектуры

---

## ❓ Контекст для AI

### Владелец проекта
- **Не программист** — весь код написан AI
- Вылавливание багов — критическая трата ресурсов
- Нужен понятный, предсказуемый код

### Ожидания от AI
1. **Всегда проверять код перед изменениями** — не предполагать
2. **Показывать план** — не писать код сразу
3. **Короткие изменения** — легче проверить и откатить
4. **Объяснять что делаешь** — владелец должен понимать

### Даты в документах
Если видишь даты **раньше октября 2025** — это галлюцинации предыдущих AI. Проекту ~2.5 месяца.

---

## 🚀 Как начать новый чат

1. Открыть новый чат
2. Приложить этот файл (`docs/PROJECT_CONTEXT.md`)
3. Описать задачу
4. Дождаться плана от AI
5. Одобрить или скорректировать

---

*Этот документ — единственный источник правды о контексте проекта.*
