# PADEL GALLERIES — Контекст проекта для AI

> **Для AI-ассистента:** Прочитай этот документ полностью перед любой работой.
> **Обновлено:** 28 декабря 2025
> **Возраст проекта:** ~3 месяца (с октября 2025)

---

## 🎯 О проекте

### Что это
Портал для сообщества падел-игроков, начинающийся с Валенсии (Испания) с планами расширения на другие города и страны.

### Целевая аудитория
Экспаты (иностранцы в Испании). Для испанцев есть Playtomic (в основном про спорт), а экспатам нужна ещё и социализация.

### Текущий функционал
1. **Фотогалереи турниров** — загрузка, просмотр, управление
2. **Распознавание лиц игроков** — InsightFace + HNSWLIB индекс + адаптивный early-exit алгоритм (v4.0)
3. **Админ-панель** — управление галереями, людьми, тегирование лиц
4. **Мультигородность** — архитектура готова (cities → locations → galleries)

### Планируемый функционал
1. **Авторизация пользователей** — Telegram/Google/Meta OAuth
2. **Социальные функции** — лайки, комментарии, избранное
3. **Бот организации игр** — интеграция существующего Telegram-бота

---

## 🏗️ Архитектура

### Единственный репозиторий
\`\`\`
NickRemizov/v0_Padel_Galleries  ← ВСЁ ЗДЕСЬ
├── app/                        # Next.js frontend (App Router)
│   └── admin/actions/         # Server Actions (people.ts, faces.ts...)
├── components/                 # React компоненты
│   └── admin/                 # ~45 компонентов админки
├── lib/                        # Утилиты, API клиент
├── python/                     # FastAPI backend (деплоится на Hetzner)
│   ├── routers/               # API endpoints (модульная структура)
│   │   ├── people/           # People API (crud, photos, consistency...)
│   │   ├── faces/            # Faces API
│   │   ├── recognition/      # Detection API (detect.py v2.3)
│   │   └── galleries.py      # Galleries API
│   ├── services/              # face_recognition.py v4.2, quality_filters.py
│   └── core/                  # responses.py (ApiResponse), exceptions.py
├── docs/                       # Документация
└── migrations/                 # SQL миграции
\`\`\`

### ⚠️ ВАЖНО: Padel_Galleries — МЁРТВЫЙ ФОРК
Репозиторий `NickRemizov/Padel_Galleries` — неудачная попытка переезда. **Игнорировать полностью.**

### Текущий стек
| Компонент | Технология | Где хостится |
|-----------|------------|--------------|
| Frontend | Next.js 15, React 19 | Vercel |
| Backend | FastAPI + Python 3.11 | Hetzner (vlcpadel.com:8001) |
| База данных | PostgreSQL | Supabase (hosted) |
| Хранилище фото | Vercel Blob | Vercel |
| Face Recognition | InsightFace + HNSWLIB | Hetzner |

---

## 🧠 Система распознавания (актуально v4.2)

### Алгоритм распознавания (v4.0 — адаптивный early exit)
\`\`\`
1. Query HNSW для кандидатов (sorted by similarity)
2. Для каждого: final_confidence = source_confidence × similarity  
3. Трекинг best_final_confidence
4. Early exit когда: similarity < best_final_confidence
\`\`\`

### Пороги (v2.3)
| Контекст | search_threshold | save_threshold |
|----------|------------------|----------------|
| С фильтрами | config (0.60) | config (0.60) |
| Без фильтров | 0.30 | config (0.60) |

**Логика:** Боксы сохраняются всегда, person_id — только если confidence >= save_threshold.

### Фильтры качества (v4.2)
- **face_size**: `max(width, height)` — фильтрация по большей стороне
- **min_detection_score**: 0.7
- **min_blur_score**: 80
- **min_face_size**: 80 px

---

## ⛔ КРИТИЧЕСКИЕ ПРАВИЛА РАЗРАБОТКИ

### Правило 1: Архитектура данных (ГЛАВНОЕ!)
\`\`\`
Frontend → Server Actions → FastAPI Backend → Supabase
\`\`\`

**ЗАПРЕЩЕНО:** Прямые вызовы Supabase из frontend-компонентов.

### Правило 2: План перед кодом
Перед ЛЮБЫМ изменением показать:
1. Какие файлы будут изменены
2. Полный flow данных
3. Дождаться одобрения

### Правило 3: Точечные правки
Если меняется <50% файла — использовать str_replace, НЕ перезаписывать целиком.

### Правило 4: API интеграция
**СНАЧАЛА** показать поля backend ответа, **ПОТОМ** писать frontend. Сверять названия полей буквально.

---

## 📁 Ключевые файлы

### Backend (Python)
| Файл | Описание | Версия |
|------|----------|--------|
| `python/routers/recognition/detect.py` | Detection + process-photo | v2.3 |
| `python/routers/people/photos.py` | Verify, unlink, batch-verify | v2.0 |
| `python/services/face_recognition.py` | Распознавание, индекс | v4.2 |
| `python/services/quality_filters.py` | Фильтры качества | max side |

### Frontend (TypeScript)
| Файл | Описание | Размер |
|------|----------|--------|
| `app/admin/actions/people.ts` | Server Actions для людей | v2.4 |
| `app/admin/actions/faces.ts` | Server Actions для лиц | |
| `components/admin/person-gallery-dialog.tsx` | Галерея игрока | 31 KB |
| `components/admin/face-tagging-dialog.tsx` | Тегирование лиц | 33 KB |
| `components/admin/gallery-images-manager.tsx` | Менеджер фото | 39 KB |

---

## 🔧 Серверы и доступы

### Hetzner (Backend)
- **URL:** http://vlcpadel.com:8001
- **Health check:** `curl http://vlcpadel.com:8001/health`
- **Деплой:** Автоматический через GitHub Actions
- **Путь:** /home/nickr/python
- **Рестарт:** /home/nickr/scripts/run.sh

### Vercel (Frontend)
- Auto-deploy при push в main

---

## 📋 Недавние изменения (декабрь 2025)

### 28 декабря
- ✅ **Batch verify** — один запрос вместо N для подтверждения фото
- ✅ **Face size max** — фильтрация по max(width, height)
- ✅ **Save threshold** — person_id только при confidence >= config

### 27 декабря  
- ✅ **detect.py v2.2** — CASE 2 распознаёт неверифицированные
- ✅ **Adaptive early exit** (v4.0) — оптимизация поиска

---

## ❓ Контекст для AI

### Владелец проекта
- **Не программист** — весь код написан AI
- Время владельца дороже других ресурсов — ошибки и переделки недопустимы

### Ожидания от AI
1. **Думать перед действием, проверять результат**
2. **АУДИТ = только анализ и отчёт** — код писать ТОЛЬКО после явного согласования
3. **Не делать того, о чём не просили**

---

*Этот документ — единственный источник правды о контексте проекта.*
