# Frontend TODO - Задачи по рефакторингу Next.js фронтенда

> Файл создан: 14.12.2025
> Последнее обновление: 14.12.2025

## Статус задач

| # | Задача | Приоритет | Статус |
|---|--------|-----------|--------|
| 1 | [Крупные компоненты: разбиение](#1-крупные-компоненты-разбиение) | 🟡 Средний | ⏳ TODO |
| 2 | [Прямой Supabase: миграция на API](#2-прямой-supabase-миграция-на-api) | 🔴 Высокий | ⏳ TODO |

---

## 1. Крупные компоненты: разбиение

**Проблема:**
Многие компоненты выросли до 500-850 строк, что затрудняет поддержку и тестирование.

### Критически крупные файлы (>20KB)

| Файл | Размер | Строк | Рекомендация |
|------|--------|-------|--------------|
| `gallery-images-manager.tsx` | 32KB | ~850 | Разбить на 4-5 компонентов |
| `database-integrity-checker.tsx` | 29KB | ~770 | Вынести проверки в хуки |
| `face-training-manager.tsx` | 29KB | ~760 | Разбить по функциональности |
| `training-stats-card.tsx` | 28KB | ~750 | Вынести графики отдельно |
| `face-tagging-dialog.tsx` | 23KB | ~610 | Разбить UI и логику |
| `person-gallery-dialog.tsx` | 22KB | ~580 | Вынести галерею отдельно |

### Средние файлы (10-20KB)

| Файл | Размер | Строк | Рекомендация |
|------|--------|-------|--------------|
| `auto-recognition-dialog.tsx` | 16KB | ~425 | Вынести прогресс-бар |
| `add-gallery-dialog.tsx` | 14KB | ~380 | Вынести форму |
| `edit-gallery-dialog.tsx` | 14KB | ~380 | Объединить с add-gallery |
| `cleanup-duplicates-button.tsx` | 14KB | ~370 | Вынести preview |
| `image-lightbox.tsx` | 12KB | ~320 | OK, сложный компонент |
| `regenerate-descriptors-dialog.tsx` | 12KB | ~310 | Вынести прогресс |
| `person-list.tsx` | 11KB | ~290 | Вынести строку таблицы |
| `avatar-selector.tsx` | 10KB | ~265 | OK |
| `gallery-card.tsx` | 10KB | ~265 | OK |
| `cities-manager.tsx` | 10KB | ~265 | OK |

### План разбиения

#### 1.1 gallery-images-manager.tsx (32KB → ~5 файлов)

\`\`\`
components/admin/gallery-images/
  index.tsx                    # Основной компонент (контейнер)
  ImageGrid.tsx                # Сетка изображений
  ImageCard.tsx                # Карточка одного изображения
  BulkActions.tsx              # Массовые действия (удалить, обработать)
  UploadProgress.tsx           # Прогресс загрузки
  hooks/
    useImageSelection.ts       # Логика выбора изображений
    useImageUpload.ts          # Логика загрузки
\`\`\`

#### 1.2 database-integrity-checker.tsx (29KB → ~4 файла)

\`\`\`
components/admin/integrity/
  index.tsx                    # Контейнер с табами
  CheckCard.tsx                # Карточка одной проверки
  CheckResults.tsx             # Результаты проверки
  FixActions.tsx               # Кнопки исправления
  hooks/
    useIntegrityCheck.ts       # Логика проверки
\`\`\`

#### 1.3 face-training-manager.tsx (29KB → ~4 файла)

\`\`\`
components/admin/training/
  index.tsx                    # Контейнер
  TrainingQueue.tsx            # Очередь обучения
  TrainingProgress.tsx         # Прогресс обучения
  TrainingHistory.tsx          # История сессий
  hooks/
    useTrainingSession.ts      # Логика сессии
\`\`\`

#### 1.4 training-stats-card.tsx (28KB → ~3 файла)

\`\`\`
components/admin/stats/
  TrainingStatsCard.tsx        # Основная карточка
  ConfidenceChart.tsx          # График confidence
  DistributionChart.tsx        # График распределения
\`\`\`

#### 1.5 face-tagging-dialog.tsx (23KB → ~3 файла)

\`\`\`
components/admin/tagging/
  FaceTaggingDialog.tsx        # Диалог
  FaceCanvas.tsx               # Canvas с лицами
  PersonSelector.tsx           # Выбор персоны
\`\`\`

### Общие паттерны рефакторинга

**1. Выделение хуков:**
\`\`\`typescript
// БЫЛО: всё в компоненте
const [data, setData] = useState([])
const [loading, setLoading] = useState(false)
const fetchData = async () => { ... }
useEffect(() => { fetchData() }, [])

// СТАЛО: отдельный хук
// hooks/useGalleryImages.ts
export function useGalleryImages(galleryId: string) {
  const [data, setData] = useState([])
  const [loading, setLoading] = useState(false)
  // ...
  return { data, loading, refetch }
}
\`\`\`

**2. Композиция вместо монолита:**
\`\`\`typescript
// БЫЛО: один огромный компонент
function GalleryImagesManager() {
  return (
    <div>
      {/* 200 строк разметки */}
    </div>
  )
}

// СТАЛО: композиция
function GalleryImagesManager() {
  return (
    <div>
      <ImageGrid images={images} onSelect={handleSelect} />
      <BulkActions selected={selected} onDelete={handleDelete} />
      <UploadProgress uploads={uploads} />
    </div>
  )
}
\`\`\`

**3. Унификация диалогов:**
\`\`\`typescript
// add-gallery-dialog.tsx и edit-gallery-dialog.tsx имеют 80% общего кода
// Объединить в:
components/admin/gallery/
  GalleryDialog.tsx            # Общий диалог
  GalleryForm.tsx              # Форма (переиспользуется)
  useGalleryForm.ts            # Логика формы
\`\`\`

### Оценка трудозатрат

| Компонент | Часы |
|-----------|------|
| gallery-images-manager | 4-6 |
| database-integrity-checker | 3-4 |
| face-training-manager | 3-4 |
| training-stats-card | 2-3 |
| face-tagging-dialog | 2-3 |
| person-gallery-dialog | 2-3 |
| Унификация add/edit диалогов | 2-3 |
| **Итого** | **18-26 часов** |

---

## 2. Прямой Supabase: миграция на API

> Детали в `python/docs/BACKEND_TODO.md` — задача #8

**Краткое резюме:**
- ~15 файлов используют `createClient()` напрямую
- Нужно перенести все операции через FastAPI
- Оценка: 22-32 часа

---

## Приоритет выполнения

1. **#2 Прямой Supabase** — архитектурная проблема
2. **#1 Крупные компоненты** — технический долг, можно делать постепенно

## Рекомендуемый порядок рефакторинга компонентов

1. `gallery-images-manager.tsx` — самый большой, часто используется
2. `database-integrity-checker.tsx` — критически важный для поддержки
3. `face-training-manager.tsx` — ключевой для ML функциональности
4. Остальные — по мере необходимости
