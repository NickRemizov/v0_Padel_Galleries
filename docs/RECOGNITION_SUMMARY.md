# 🧠 Face Recognition — Краткое резюме

> **Для:** Николай (владелец проекта)
> **Обновлено:** 28 декабря 2025
> **Версия алгоритма:** v4.2

---

## Как это работает (простыми словами)

\`\`\`
Фото загружено → InsightFace находит лица → Каждое лицо = 512 чисел (эмбеддинг)
                                                    ↓
                                    Сравниваем с известными игроками (HNSW индекс)
                                                    ↓
                            final_confidence = source_confidence × similarity
                                                    ↓
                            >= 60%? → Присваиваем игроку
                            < 60%? → Бокс сохраняем, person_id = NULL
\`\`\`

---

## Ключевые понятия

| Термин | Что это | Где хранится |
|--------|---------|--------------|
| **Эмбеддинг** | 512 чисел, описывающих лицо | `photo_faces.insightface_descriptor` |
| **BBox** | Координаты лица на фото | `photo_faces.insightface_bbox` |
| **Face Size** | max(width, height) бокса | Вычисляется, не хранится |
| **Confidence** | Уверенность распознавания (0-1) | `photo_faces.recognition_confidence` |
| **Verified** | Подтверждено вручную | `photo_faces.verified` |
| **Excluded** | Исключён из индекса (outlier) | `photo_faces.excluded_from_index` |

---

## Пороги и фильтры (v2.3 + v4.2)

### Пороги распознавания
| Режим | search_threshold | save_threshold | Результат |
|-------|------------------|----------------|----------|
| С фильтрами | 0.60 (config) | 0.60 (config) | Стандартное распознавание |
| Без фильтров | 0.30 | 0.60 (config) | Видим кандидатов в top_matches, но не сохраняем |

### Фильтры качества
| Фильтр | Значение | Как работает |
|--------|----------|-------------|
| Min Face Size | 80 px | **max(width, height)** >= 80 |
| Min Blur Score | 80 | Laplacian variance |
| Min Detection Score | 0.7 | Уверенность InsightFace |

---

## Бейджи на фото (что означают)

| Бейдж | Цвет | Что это значит |
|-------|------|----------------|
| *Без бейджа* | — | Новое фото, ещё не обработано |
| **NFD** | Серый | No Faces Detected — лиц не найдено |
| **2/4** | Оранжевый | 2 неопознанных из 4 лиц |
| **75%** | Синий | Минимальная уверенность распознавания |
| **✓** | Зелёный | Все лица подтверждены |

---

## Алгоритм распознавания (v4.0 — Adaptive Early Exit)

\`\`\`python
# Для каждого кандидата из HNSW (sorted by similarity):
final_confidence = source_confidence × similarity

# Где source_confidence:
# - Verified face: 1.0
# - Unverified face: recognition_confidence из БД

# Early exit когда:
if similarity < best_final_confidence:
    break  # Дальнейшие кандидаты не могут улучшить результат
\`\`\`

**Преимущества:**
- Verified лица приоритетнее
- Цепочки confidence затухают естественно
- Быстрый выход без лишних итераций

---

## Процесс работы администратора

### 1. Загрузка фото
- Drag & Drop или кнопка "Загрузить"

### 2. Пакетное распознавание
- Кнопка "Распознать фото" 🔍
- Галочка "Применять настройки качества" (рекомендуется)

### 3. Работа с неизвестными лицами
- Кнопка "Неизвестные лица" 👤+
- Кластеры похожих лиц
- Действия: создать игрока / выбрать существующего / зрители / удалить

### 4. Ручная верификация
- Клик на фото → выбрать игрока для каждого лица
- После сохранения → бейдж ✓

### 5. Галерея игрока — Batch Verify ⚡ NEW
- Кнопка "Подтвердить все фото" — один запрос вместо N
- Мгновенная операция для любого количества фото

---

## API эндпоинты (ключевые)

| Метод | Эндпоинт | Описание | Версия |
|-------|----------|----------|--------|
| POST | `/recognition/detect-faces` | Детекция лиц | |
| POST | `/recognition/process-photo` | Детекция + распознавание | v2.3 |
| POST | `/people/{id}/verify-on-photo` | Верификация одного | |
| POST | `/people/{id}/batch-verify-on-photos` | Batch верификация | v2.0 NEW |
| GET | `/people/{id}/embedding-consistency` | Аудит эмбеддингов | |

---

## Частые вопросы

### Почему лицо не распознаётся?
1. Слишком маленькое (< 80 px по большей стороне)
2. Размытое (blur < 80)
3. Игрока нет в базе
4. Confidence < 60% (видно в top_matches, но не сохраняется)

### Почему показывает одно число, а фильтрует по другому?
**Решено в v4.2:** Теперь везде используется max(width, height).

### Как улучшить распознавание игрока?
1. Подтвердить (verify) хорошие фото
2. Исключить outliers через аудит консистентности
3. Индекс обновляется автоматически

---

## Архитектура (очень кратко)

\`\`\`
Frontend (Next.js)
    ↓
Server Actions (app/admin/actions/)
    ↓
FastAPI (vlcpadel.com:8001)
    ↓
Supabase (photo_faces таблица)
\`\`\`

---

*Последнее обновление: 28 декабря 2025*
