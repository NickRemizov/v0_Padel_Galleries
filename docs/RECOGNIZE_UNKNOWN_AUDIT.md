# Аудит: конвейер recognize-unknown

## Краткие выводы
- Индекс перестраивается только после завершения всей партии, поэтому ранние распознавания не видны поздним в том же запуске.
- Пагинация по offset для неизвестных лиц пропускает или дублирует строки, если записи обновляются во время обработки.
- Загрузчики неизвестных лиц в роутере и в Supabase-сервисе расходятся по фильтрам и сортировке, давая разные входные наборы.
- Перестройка индекса идет по `created_at`, без дедупликации дескрипторов и без ограничения емкости, из-за чего цепная декорреляция confidence усиливается старыми дубликатами.
- HDBSCAN запускается без детерминированного seed и на изменяющихся данных, поэтому состав кластеров плавает между запусками.

## Найденные баги и их привязка к симптомам
1) **Сталый индекс внутри итерации** — ранние автоприсвоения не добавляются в индекс до конца партии, поэтому следующие лица ищут по старому состоянию и получают другие персоны (Симптомы 1–2).
2) **Offset-пагинация на изменяемом наборе** — записи исчезают из выборки после обновления `person_id`, сдвигая offset и создавая пропуски/дубли (Симптомы 1,3).
3) **Разные фильтры неизвестных** — пагинированный загрузчик не проверяет `verified=False`/`excluded_from_index`, тогда как сервисный — проверяет; разные входные наборы ведут к нестабильным результатам (Симптомы 1–2).
4) **Перегруженный индекс без дедупликации** — в индекс попадают все дескрипторы (включая авто) по `created_at` без ограничений, усиливая цепной распад confidence и держа старые дубликаты (Симптомы 2,4).
5) **Нондетерминированная кластеризация** — HDBSCAN работает без seed на постоянно меняющемся наборе, поэтому кластеры "плавают" (Симптом 4).

## Root cause analysis
- **Симптом 1 (нестабильные результаты между запусками):** сочетание устаревшего индекса внутри партии и смещений offset после обновлений `person_id` даёт разные входы/выходы; различия фильтров между маршрутом и сервисом добавляют вариативности.
- **Симптом 2 (мусорные кластеры присваиваются разным людям):** мусорные дескрипторы остаются в индексе, а цепной распад confidence умножается на большое число дублей; отсутствие инкрементального обновления заставляет алгоритм выбирать разные опорные лица в одном прогоне.
- **Симптом 3 (ошибки в количестве дескрипторов):** offset-пагинация по изменяемому множеству теряет или дублирует записи, поэтому статусы и подсчёты расходятся между страницами.
- **Симптом 4 (нестабильная кластеризация):** входной набор меняется между прогоном из-за обновлений person_id и фильтров, плюс отсутствие seed в HDBSCAN даёт разные разбиения даже при одинаковых данных.

## Рекомендации
1. **Инкрементальное обновление индекса**: добавлять каждое принятое распознавание в HNSW до обработки следующего лица; батчевый rebuild — только при сбое. Сложность: средняя; риск: низкий/средний.
2. **Cursor-пагинация**: заменить offset на курсор по `created_at > last_seen` (или `id`) в роутере и сервисе, перечитывать партию после обновлений. Сложность: средняя; риск: средний из-за изменения API.
3. **Унификация фильтров неизвестных**: вынести единый загрузчик с `verified=False`, `excluded_from_index=False` и детерминированным order. Сложность: низкая; риск: низкий.
4. **Гигиена индекса**: дедуплицировать дескрипторы (например, оставлять последний per face) и ограничить объем или ребалансировать веса по состоянию верификации, чтобы снижать эффект цепного распада confidence. Сложность: средняя; риск: средний.
5. **Детерминизм кластеризации**: задавать фиксированный seed/random_state для HDBSCAN и фиксировать порядок входных данных; опционально — снимок набора неизвестных перед кластеризацией. Сложность: низкая; риск: низкий.

## Приоритизация
1. Инкрементальное обновление индекса (останавливает неправильные назначения в пределах одного запуска).
2. Cursor-пагинация с едиными фильтрами (убирает пропуски/дубли).
3. Гигиена индекса (уменьшает колебания confidence и распространение мусора).
4. Детерминизм кластеризации (стабилизирует размер кластеров для ревью).

## Статус исправлений

### ✅ Реализовано (20.12.2025)

1. **Инкрементальное обновление индекса**
   - `HNSWIndex.add_embeddings()` — добавление эмбеддингов без полного rebuild
   - `FaceRecognitionService.add_face_to_index()` — facade метод
   - В `recognize-unknown` каждое успешное распознавание сразу добавляется в индекс

2. **Cursor-пагинация**
   - Заменён offset на курсор `(created_at, id)`
   - Избежание пропусков/дублей при обновлении person_id

### 🔜 TODO

3. **Унификация фильтров** — вынести единый загрузчик
4. **Гигиена индекса** — дедупликация, ограничение объёма
5. **Детерминизм HDBSCAN** — фиксированный seed
