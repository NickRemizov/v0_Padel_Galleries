# Migration Scripts - Supabase → Self-hosted PostgreSQL + Vercel Blob → MinIO

Эти скрипты выполняют полную миграцию данных проекта Galleries на собственный сервер.

## Что мигрируется

### База данных (Supabase → PostgreSQL 17)
- ✅ 17 таблиц с полной структурой
- ✅ Индексы и ограничения
- ✅ Vector extension для pgvector
- ❌ Legacy-поля Face-API (bounding_box, descriptor, confidence) - **НЕ** переносятся

### Файлы (Vercel Blob → MinIO)
- ✅ Все изображения галерей
- ✅ Аватары людей
- ✅ Фотографии пользователей
- ✅ Обложки галерей

## Предварительные требования

1. **PostgreSQL 17** установлен и запущен
2. **MinIO** установлен и запущен
3. **Bucket "galleries"** создан в MinIO с публичным доступом
4. **Node.js 18+** установлен

## Шаг 1: Подготовка

### 1.1 Установка зависимостей

\`\`\`bash
cd migration-scripts
npm install
\`\`\`

### 1.2 Настройка переменных окружения

Скопируй `.env.example` в `.env`:

\`\`\`bash
cp .env.example .env
\`\`\`

Заполни переменные:

\`\`\`bash
# Получи Supabase DB URL из проекта v0
SUPABASE_DB_URL=postgres://postgres:PASSWORD@db.jczmumbzpqlckbkgznsd.supabase.co:5432/postgres?sslmode=require

# Твой локальный PostgreSQL
TARGET_DATABASE_URL=postgresql://galeries_user:galeries_strong_pass_2025@127.0.0.1:5432/galleries

# Получи Blob token из проекта v0
BLOB_READ_WRITE_TOKEN=vercel_blob_...

# MinIO настройки
S3_ENDPOINT=http://23.88.61.20:9200
S3_ACCESS_KEY=galleries-app
S3_SECRET_KEY=galleries_secret_2025_strong_key
S3_BUCKET=galleries
S3_PUBLIC_URL=https://api.vlcpadel.com/s3/galleries
\`\`\`

## Шаг 2: Миграция схемы базы данных

Создай все таблицы в новой базе:

\`\`\`bash
npm run migrate:schema
\`\`\`

**Что произойдет:**
- Создастся 17 таблиц
- Установятся все индексы
- Создадутся ENUM типы
- Настроятся базовые RLS политики

**Проверка:**

\`\`\`bash
psql $TARGET_DATABASE_URL -c "\dt"
\`\`\`

Должно показать все 17 таблиц.

## Шаг 3: Миграция данных

Перенеси все данные из Supabase:

\`\`\`bash
npm run migrate:data
\`\`\`

**Что произойдет:**
- Данные копируются пакетами по 100 строк
- Legacy-поля пропускаются
- Показывается прогресс миграции
- В конце выводится сводка

**Ожидаемое время:** 5-15 минут в зависимости от объема данных

**Проверка:**

\`\`\`bash
psql $TARGET_DATABASE_URL -c "SELECT COUNT(*) FROM galleries;"
psql $TARGET_DATABASE_URL -c "SELECT COUNT(*) FROM gallery_images;"
psql $TARGET_DATABASE_URL -c "SELECT COUNT(*) FROM people;"
\`\`\`

## Шаг 4: Миграция файлов

Перенеси все изображения в MinIO:

\`\`\`bash
npm run migrate:files
\`\`\`

**Что произойдет:**
- Файлы скачиваются с Vercel Blob
- Загружаются в MinIO (структура: YYYY/MM/filename)
- URL обновляются в базе данных
- Показывается прогресс

**Ожидаемое время:** 30-60 минут в зависимости от количества файлов

**Проверка:**

Открой MinIO консоль: http://23.88.61.20:9201
- Логин: `minioadmin`
- Пароль: `2o5CBBoM/ynAEcrcxViXmvcqDs4UAFXj`

Проверь что в bucket `galleries` появились файлы.

## Шаг 5: Миграция все-в-одном (опционально)

Если хочешь запустить все шаги сразу:

\`\`\`bash
npm run migrate:all
\`\`\`

⚠️ **Внимание:** Это займет много времени (1-2 часа). Лучше делать по шагам.

## Проверка миграции

### Проверка базы данных

\`\`\`bash
# Подключись к новой базе
psql $TARGET_DATABASE_URL

# Проверь количество записей
SELECT 
  schemaname, 
  tablename, 
  n_tup_ins AS inserted_rows
FROM pg_stat_user_tables
WHERE schemaname = 'public'
ORDER BY tablename;

# Проверь что vector extension работает
SELECT COUNT(*) FROM photo_faces WHERE insightface_descriptor IS NOT NULL;

# Выход
\q
\`\`\`

### Проверка файлов

\`\`\`bash
# Проверь что URL обновлены
psql $TARGET_DATABASE_URL -c "SELECT image_url FROM gallery_images LIMIT 5;"
\`\`\`

URL должны начинаться с `https://api.vlcpadel.com/s3/galleries/`

## Откат (если что-то пошло не так)

### Очистка базы данных

\`\`\`bash
psql $TARGET_DATABASE_URL << EOF
DROP SCHEMA public CASCADE;
CREATE SCHEMA public;
GRANT ALL ON SCHEMA public TO galeries_user;
EOF
\`\`\`

Затем повтори миграцию с шага 2.

### Очистка MinIO

В консоли MinIO (http://23.88.61.20:9201):
1. Выбери bucket `galleries`
2. Удали все файлы
3. Повтори шаг 4

## Статистика миграции

После успешной миграции ты увидишь что-то вроде:

\`\`\`
📊 Migration Summary:
═══════════════════════════════════════
Table                Source    Target   Skipped   Time (ms)
───────────────────────────────────────
photographers            5         5         0        156
locations               12        12         0        234
galleries              156       156         0       1245
gallery_images        3806      3806         0      15678
people                  89        89         0        567
photo_faces          12456     12456         0      45678
...
═══════════════════════════════════════
TOTAL                16789     16789         0
\`\`\`

## Часто задаваемые вопросы

### 1. Что делать если миграция прервалась?

Скрипты поддерживают `ON CONFLICT DO UPDATE`, так что можно просто перезапустить:

\`\`\`bash
npm run migrate:data
\`\`\`

### 2. Можно ли мигрировать отдельную таблицу?

Да, отредактируй `002_migrate_data.ts` и закомментируй ненужные таблицы.

### 3. Что делать если файлы не загружаются в MinIO?

Проверь:
- MinIO работает: `curl http://localhost:9200/minio/health/live`
- Bucket существует: `docker exec minio mc ls local/`
- Правильные креденшалы в `.env`

### 4. Legacy-поля точно не нужны?

Да, они от старой Face-API системы. Сейчас используется InsightFace с полями:
- `insightface_descriptor` (vector 512)
- `insightface_confidence`
- `insightface_bbox`

## Следующие шаги

После успешной миграции:

1. ✅ Обнови код приложения для работы с новой базой
2. ✅ Настрой NextAuth.js для аутентификации
3. ✅ Обнови FastAPI для работы с новой базой
4. ✅ Протестируй приложение локально
5. ✅ Задеплой на сервер

## Поддержка

Если что-то пошло не так:
1. Проверь логи скриптов
2. Проверь переменные окружения
3. Проверь что PostgreSQL и MinIO работают
4. Напиши мне - разберемся вместе!
