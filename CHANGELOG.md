# Changelog

Все значимые изменения в проекте документируются в этом файле.

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/),
и проект придерживается [Semantic Versioning](https://semver.org/lang/ru/).

> **Примечание:** Проект начат в октябре 2025. Даты в ранних записях были автоматически сгенерированы и не отражают реальную хронологию. Записи сохранены для документирования изменений.

---

## [Unreleased]

### В работе
- Миграция frontend на централизованный FastAPI backend
- Рефакторинг крупных компонентов (>20KB)

---

## [6.1.1] - 10 Января 2026

### 🔴 P0/P1 Audit Fixes — Index Sync

Исправлены критические проблемы рассинхронизации HNSW индекса с БД.

#### P0 — Критические

| Проблема | Файл | Исправление |
|----------|------|-------------|
| `set-excluded` удалял неопознанное лицо из БД, но не из индекса | `recognition.py:311` | Добавлен `remove_face_from_index()` перед удалением из БД |
| `set-excluded` использовал remove/add вместо update_metadata | `recognition.py:340` | Заменён на `update_face_metadata(excluded=...)` |
| `recognize-unknown` не обновлял metadata после присвоения person_id | `recognition.py:168` | Добавлен `update_face_metadata()` сразу после DB update |

#### P1 — Важные

| Проблема | Файл | Исправление |
|----------|------|-------------|
| `clear-descriptor` не удалял неизвестные лица из индекса | `recognition.py:266` | Условие `had_person_id` → `had_descriptor` |
| `batch-verify` обновлял metadata только при смене person_id | `batch_operations.py:191` | Убрано условие, metadata обновляется всегда |
| `selfie.py` создавал собственный FaceRecognitionService | `selfie.py:30` | Использует инжектированный `face_service_instance` |

#### Отложено (P2)
- `get_unique_people_count()` считает None как отдельного человека
- N+1 запросы в `_get_face_metrics()`

---

## [6.1.0] - 10 Января 2026

### 🔧 Audit Fixes

#### Исправлено
- **Singleton в user router**: `photo_faces.py` теперь использует инжектированный `face_service_instance` вместо создания нового экземпляра на каждый запрос
- **auto-recognize sync**: `processing.py` теперь вызывает `update_face_metadata()` после обновления person_id в БД
- **Empty index handling**: `HNSWIndex.initialize_empty()` для graceful старта с пустой БД
- **k=0 в query()**: Добавлена проверка на пустой индекс в `query()` для предотвращения crash

#### Добавлено
- `user.set_services(face_service)` в `main.py`
- `face_service_instance` и `set_services()` в `routers/user/__init__.py`

---

## [6.0.0] - Январь 2026

### 🚀 Variant C Architecture - HNSW Index Redesign

#### Изменено
- **ВСЕ лица с дескрипторами теперь в индексе** (независимо от person_id)
- `excluded_from_index` теперь метаданные в индексе, не фильтр при загрузке
- `hidden_by_user` НЕ влияет на индекс (hidden фото используются для распознавания)

#### Добавлено
- `HNSWIndex.update_metadata()` - обновление person_id/verified/excluded БЕЗ rebuild
- `HNSWIndex.excluded_map` - карта исключённых лиц
- `FaceRecognitionService.update_face_metadata()` - высокоуровневый метод
- `EmbeddingsRepository` возвращает 6-tuple с excluded_flags

#### Преимущества
- Изменение person_id не требует rebuild индекса
- Быстрее: `update_metadata()` вместо remove+add
- Консистентность: один индекс, метаданные обновляются отдельно

#### Файлы изменены
- `services/hnsw_index.py` - excluded_map, update_metadata()
- `services/supabase/embeddings.py` - 6-tuple, без фильтров
- `services/face_recognition.py` - recognize_face скипает None/excluded
- `routers/recognition/detect.py` - add all faces, use update_metadata
- `routers/user/photo_faces.py` - verify/reject через update_metadata
- `routers/faces/batch_operations.py` - batch-assign/verify через update_metadata
- `routers/images/processing.py` - auto-recognize с index sync

---

## [1.1.0] - Декабрь 2025

### 🔴 КРИТИЧЕСКИЙ РЕФАКТОРИНГ - ФАЗА 1 ГИБРИДНОЙ АРХИТЕКТУРЫ

#### Добавлено
- **Гибридная архитектура**: Работа с лицами теперь идет через FastAPI backend
  - `python/routers/faces.py` - Роутер `/api/faces` для сохранения, обновления и удаления лиц
  - Автоматическое обновление индекса распознавания при сохранении верифицированных лиц
  - FastAPI - единый источник правды для `photo_faces`
- **Улучшенные паттерны Supabase**
  - `lib/supabase/with-supabase.ts` - Декоратор для server actions
  - `lib/types/result.ts` - Стандартный тип `Result<T>` для единообразной обработки ошибок
- **Улучшенное логирование**
  - `lib/logger.ts`: добавлена проверка NODE_ENV, debug логи только в development

#### Улучшено
- `app/admin/actions.ts`: `savePhotoFaceAction` вызывает FastAPI вместо прямой записи в Supabase
- `lib/supabase/server.ts`: теперь бросает ошибку вместо возврата null
- `lib/supabase/client.ts`: убраны non-null assertions, добавлены проверки

#### Исправлено
- **Проблема с индексом распознавания**: Идентичные фото теперь распознаются с 100% вместо 81%

#### Архитектура (Гибридная)
**Через FastAPI:**
- ✅ Распознавание лиц
- ✅ Сохранение `photo_faces`
- ✅ Обучение модели
- ✅ Кластеризация

**Напрямую через Supabase:**
- Galleries CRUD
- People CRUD
- Metadata
- Публичные запросы

---

## [1.0.0] - Декабрь 2025

### Добавлено
- Унифицированная система логирования (lib/logger.ts)
- Утилита debounce (lib/debounce.ts) для оптимизации автосохранений
- Retry механизм с exponential backoff в API client

### Улучшено
- API client: добавлен retry с задержками 1s → 2s → 4s
- deletePersonAction: удаляет orphaned face records и перестраивает индекс
- getRecognitionStatsAction: оптимизирован N+1 query
- gallery-images-manager: улучшена обработка ошибок при batch upload
- face-tagging-dialog: добавлен debounce 500ms на автосохранение

### Исправлено
- Race condition при batch удалении данных галереи
- Orphaned face descriptors при удалении игроков
- Множественные одновременные запросы при быстром переключении

---

## [0.8.x] - Ноябрь 2025

### Добавлено
- Клик на картинку галереи открывает галерею управления фотографиями
- Динамическая кнопка удаления в галерее игрока
- Информация под фото: название файла и название галереи с датой
- Sticky header в галерее событий
- Удаление выбранных фото с подтверждением

### Исправлено
- Квадратные скобки в названии галереи
- Tooltip на кнопке сохранения в диалоге тегирования
- AlertDialog для удаления файлов
- Логика верификации лиц

---

## [0.7.x] - Ноябрь 2025

### Добавлено
- Полная документация проекта (ARCHITECTURE.md, CHANGELOG.md, CONTRIBUTING.md)
- Проверка целостности базы данных (15 проверок, 7 автоисправлений)

### Улучшено
- UI галереи игрока
- Отображение статусов верификации
- Навигация между фотографиями

---

## [0.5.x - 0.6.x] - Ноябрь 2025

### Добавлено
- Инструкции по развертыванию FastAPI на Hetzner
- Автоматические скрипты деплоя

### Исправлено
- Проблемы с CORS в FastAPI
- Обработка ошибок при загрузке изображений

---

## [0.3.x - 0.4.x] - Октябрь-Ноябрь 2025

### Добавлено
- Кластеризация неизвестных лиц с HDBSCAN
- Пакетное подтверждение кластеров
- Автоматическое распознавание лиц с InsightFace
- HNSWLIB индекс для быстрого поиска
- Настройки качества распознавания

---

## [0.1.x - 0.2.x] - Октябрь 2025

### Добавлено
- Telegram бот для отправки фотографий
- Управление людьми (игроками)
- Профили с рейтингами и соцсетями
- Базовая структура проекта
- Управление галереями событий
- Загрузка фотографий в Vercel Blob
- Supabase интеграция
- Google OAuth аутентификация
